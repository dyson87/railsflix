<h1>List of Movies</h1><br>

<% @movies.each do |movie| %>
    <li>
        <%= link_to(movie.title, "/movies/#{movie.id}") %><br><br>
    </li>
    <% end %>

<%= link_to("Back to Homepage", root_path)%>